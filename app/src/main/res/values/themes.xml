<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NoteSample" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#64B5F6</item>
        <item name="colorPrimaryVariant">#2196F3</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#64B5F6</item>
        <item name="colorSecondaryVariant">#2196F3</item>
        <item name="colorOnSecondary">#FFFFFF</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnSecondary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- styles.xml -->
    <style name="YourButtonTheme" parent="Theme.AppCompat.Light"> <!-- Adjust parent theme as per your app's theme -->
        <!-- Customize attributes specific to your button -->
        <item name="android:textColor">@android:color/white</item> <!-- Text color -->
        <item name="android:background">@drawable/white_outline_button</item> <!-- Background drawable -->
        <!-- Add more customizations if needed -->
    </style>

    <!-- styles.xml -->
    <style name="YourTabTheme" parent="Theme.AppCompat.Light"> <!-- Adjust parent theme as per your app's theme -->
        <!-- Customize attributes specific to your button -->
        <item name="android:textColor">@android:color/white</item> <!-- Text color -->
        <!--        <item name="android:background">@drawable/white_outline_button</item> &lt;!&ndash; Background drawable &ndash;&gt;-->
        <!-- Add more customizations if needed -->
    </style>

    <!-- styles.xml -->
    <style name="homeButtonTheme" parent="Theme.AppCompat.Light"> <!-- Adjust parent theme as per your app's theme -->
        <!-- Customize attributes specific to your button -->
        <item name="android:textColor">@android:color/holo_blue_light</item> <!-- Text color -->
        <item name="android:background">@drawable/blue_outline_button</item> <!-- Background drawable -->
        <!-- Add more customizations if needed -->
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <!-- Customize popup overlay attributes if needed -->
        <item name="android:colorBackground">@color/white</item>
        <!-- Other popup overlay customizations -->
    </style>


</resources>