<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- White background -->
    <stroke
        android:width="2dp"
        android:color="@color/blue" /> <!-- Blue border -->
    <corners android:radius="8dp" /> <!-- Rounded corners -->
</shape>
